# Components (copy/paste)

This folder is an optional library of reusable HTML snippets.

- These are **not automatically included** (no bundler is set up).
- Copy/paste sections into pages under `public/` as you build.

